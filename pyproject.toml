[project]
name = "mini-transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gleb Stepanov", email = "gleb.stepanov@online.de" }
]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
    "pytest>=9.1.1",
    "torch>=2.13.0",
]

[project.scripts]
mini-transformer = "mini_transformer:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
